default: &default
  adapter: postgresql
  encoding: unicode
  pool: 5

development:
  <<: *default
  database: quasars_development

test:
  <<: *default
  database: quasars_test

production:
  url: <%= ENV["DATABASE_URL"] %>
